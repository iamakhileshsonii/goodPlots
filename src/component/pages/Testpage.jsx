import React from 'react';
import useData from '../../hooks/useData';

const Testpage = () => {
  const gpData = useData();
  console.log(gpData)
    
  return (
    <div>
        

    </div>
  )
}

export default Testpage;
